$\pi^*$ (Physical Intelligence) 价值函数训练与归一化技术总结

本文档总结了关于 $\pi^*$ 论文（2511.14759）中价值函数（Value Function, VF）的定义、归一化逻辑以及在处理失败轨迹时的工程实现细节。

1. 核心定义：奖励函数与价值目标

在 $\pi^*$ 中，价值函数的目标是预测距离任务成功的负步数。

奖励函数 (Reward Function)

对于每一回合（Episode），奖励定义如下：

$r_t = 0$：当 $t = T$ 且任务成功（Success）。

$r_t = -C_{fail}$：当 $t = T$ 且任务失败（Failure）。

$r_t = -1$：所有其他中间步骤。

价值函数目标 ($V_{target}$)

价值函数旨在通过函数近似（Function Approximation）预测累计回报：

成功轨迹：$V(s_t) = -(T - t)$ （即距离成功的剩余步数的负值）。

失败轨迹：$V(s_t) = -(T - t) - C_{fail}$ （由于 $C_{fail}$ 很大，该值极低）。

2. 归一化策略 (Normalization Strategy)

为了在不同长度的任务中保持一致性，并适应 Transformer (Gemma 3) 的数值敏感性，价值被归一化到 $(-1, 0)$。

计算公式

$$V_{norm} = \text{Clip} \left( \frac{V_{raw}}{T_{max}}, \min=-1, \max=0 \right)$$

$T_{max}$ (Task-specific)：该任务在数据集中的最大步数（通常取 95th 百分位数）。

$C_{fail}$：设定为一个大常数，确保 $\frac{-(T-t) - C_{fail}}{T_{max}} \le -1$。

3. 失败轨迹的工程实现 (The "All -1" Problem)

标签设定 (Labels)

在训练数据集中，每一帧（Time-step）的标签设定逻辑如下：

轨迹类型

时间步 $t$ 的标签 ($y_t$)

直观解释

成功 (Success)

$-\frac{\text{remaining\_steps}}{T_{max}}$

从某个负值（如 $-0.8$）平滑增长到 $0$。

失败 (Failure)

$-1.0$

全程固定为最小值 $-1.0$。

为什么模型能学到“价值下降”？

尽管失败轨迹的标签是平整的（$-1.0$），但模型在推理时会表现出 Value Drop（如论文图 4 所示）：

期望值预测：模型在训练时同时看到成功和失败的样本。在错误发生前，视觉特征与成功样本相似，模型预测值较高（处于期望值）。

特征识别：一旦发生错误（如物体掉落），视觉特征进入“仅在失败样本中出现”的范畴，模型预测值立刻跌向训练中对应的目标值 $-1.0$。

4. 实验准则与最佳实践

数值建议

$T_{max}$：必须按任务动态设置。如果全用统一的大 $T_{max}$，短任务的价值动态范围会因被过度压缩而消失。

$C_{fail}$：只需确保 $C_{fail} \ge T_{max}$ 即可实现有效的边界截断。

Loss Function：建议使用 Huber Loss。它在处理从正常状态跌入 $-1.0$ 的剧烈梯度时比 MSE 更稳健，能防止模型参数更新过激。

物理智能 (PI) 的视角

这种将价值函数约束在 $[-1, 0]$ 的方法，本质上是将复杂的“生存建模”简化为了**“进度评估”与“风险规避”**的结合体。

5. 可视化理解 (Figure 4 逻辑)

绿色部分 (Increase)：表示模型预测当前动作正在缩短与目标的距离，进展顺利。

红色部分 (Decrease)：表示模型预测当前状态已脱离成功序列，进入了最终会导致失败的概率分布。

注：本文档基于对 Physical Intelligence 团队 $\pi^$ 架构的深度分析。*

